{
  "name": "flowchart-fun-deta",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": "true",
  "workspaces": [
    "app"
  ],
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "deta": "^0.0.10",
    "express": "^4.17.1"
  },
  "scripts": {
    "build": "yarn workspace app build",
    "start": "yarn workspace app start",
    "deploy": "yarn build && deta deploy"
  }
}
